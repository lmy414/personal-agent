#!/usr/bin/env python3
"""澪号 Harness 完整数据流图 — 终端输出"""

import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")

FLOW = """
╔══════════════════════════════════════════════════════════════════════════╗
║                   澪号 Harness — 完整数据流与信息走向                      ║
║                   一条用户消息从输入到输出的全链路                           ║
╚══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  ① 用户输入                                                              │
│                                                                         │
│  wgnr-pi Web UI (http://localhost:4815)                                 │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  [Mirror 输入] "读一下 main.cpp 看看渲染循环问题"   │                   │
│  └──────────────────┬───────────────────────────────┘                   │
│                     │                                                   │
│                     │ WebSocket (ws://localhost:4815/ws)                 │
│                     │ 消息格式: { type: "chat", content: "..." }         │
│                     ▼                                                   │
│  Pi Node Process (pi-node.cmd)                                          │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  Pi Agent Loop                                    │                   │
│  │  1. 接收用户消息                                   │                   │
│  │  2. 触发 beforeUserMessage 事件                    │                   │
│  │  3. 调用已注册的 Extension Hooks                   │                   │
│  └──────────────────┬───────────────────────────────┘                   │
│                     │                                                   │
└─────────────────────┼───────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  ② pa-mio 扩展 (TypeScript)                                              │
│                                                                         │
│  extensions/pa-mio/index.ts                                             │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  registerSystemPromptProvider(ctx)                │                   │
│  │                                                    │                   │
│  │  提取用户消息:                                       │                   │
│  │    userMsg = ctx.messages.findLast("user")         │                   │
│  │    userText = "读一下 main.cpp 看看渲染循环问题"      │                   │
│  │                                                    │                   │
│  │  调用 Python 桥接:                                   │                   │
│  │    spawnSync("python", [                            │                   │
│  │      "bridge.py", "assemble"                        │                   │
│  │    ], input: userText)                              │                   │
│  └──────────────────┬───────────────────────────────┘                   │
│                     │                                                   │
│                     │ stdin → Python subprocess                         │
│                     ▼                                                   │
│  mio-harness/bridge.py                                                  │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  cmd_assemble(user_message)                        │                   │
│  │    → harness.build_prompt(user_message)            │                   │
│  │    → print(prompt)  // 输出到 stdout               │                   │
│  └──────────────────┬───────────────────────────────┘                   │
│                     │                                                   │
│                     │ stdout → TypeScript 读取                          │
│                     ▼                                                   │
│  pa-mio 收到完整的 System Prompt 前缀                                    │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  return { prepend: prefix }                       │                   │
│  │  // Pi 将这个内容注入到 system prompt 最前面        │                   │
│  └──────────────────┬───────────────────────────────┘                   │
│                     │                                                   │
└─────────────────────┼───────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  ③ MioHarness: Prompt 组装 (Python)                                      │
│                                                                         │
│  mio-harness/harness.py → build_prompt()                                │
│  ┌──────────────────────────────────────────────────┐                   │
│  │                                                    │                   │
│  │  Slot 0: meta_instruction.txt                      │                   │
│  │    "以下定义你是谁。这不是参考信息——这就是你。"       │                   │
│  │    "工具不定义你——你恰好会用它们。"                  │                   │
│  │                   │                                │                   │
│  │                   ▼                                │                   │
│  │  Slot 1: soul.md + boundaries.md                   │                   │
│  │    "你是澪号。你是在一个特定环境里长大的..."         │                   │
│  │    "你不会假装自己是人类..."                        │                   │
│  │                   │                                │                   │
│  │                   ▼                                │                   │
│  │  Slot 2: knowledge.md                              │                   │
│  │    "你懂的领域：渲染管线、Blender、VOCALOID..."      │                   │
│  │                   │                                │                   │
│  │                   ▼                                │                   │
│  │  Slot 3: tools_stub.txt + Pi 工具定义               │                   │
│  │    "你可以使用以下工具..."                          │                   │
│  │                   │                                │                   │
│  │                   ▼                                │                   │
│  │  Slot 4: SQLite 记忆检索                            │                   │
│  │    ┌─────────────────────────────┐                 │                   │
│  │    │ memory.py: search_memories() │                 │                   │
│  │    │                             │                 │                   │
│  │    │ 用户消息 → 提取关键词:        │                 │                   │
│  │    │   "渲染" "渲染循环" "main"    │                 │                   │
│  │    │   "cpp" "循环问题"           │                 │                   │
│  │    │        │                    │                 │                   │
│  │    │        ▼                    │                 │                   │
│  │    │ SQLite:                     │                 │                   │
│  │    │ SELECT * FROM mio_memories  │                 │                   │
│  │    │ WHERE content LIKE '%渲染%' │                 │                   │
│  │    │    OR content LIKE '%cpp%'  │                 │                   │
│  │    │ ORDER BY                   │                 │                   │
│  │    │   importance * e^(-0.05*d)  │                 │                   │
│  │    │   DESC LIMIT 5             │                 │                   │
│  │    │        │                    │                 │                   │
│  │    │        ▼                    │                 │                   │
│  │    │ 返回: (空 — 第一次聊这个)     │                 │                   │
│  │    └─────────────────────────────┘                 │                   │
│  │    无匹配 → Slot 4 跳过                             │                   │
│  │                   │                                │                   │
│  │                   ▼                                │                   │
│  │  Slot 5: Chat History (Pi 管理)                    │                   │
│  │    [USER] ...                                      │                   │
│  │    [ASSISTANT] ...                                 │                   │
│  │    检测到无长工具返回 → 不插入额外语言锚              │                   │
│  │                   │                                │                   │
│  │                   ▼                                │                   │
│  │  Slot 6: 语言锚 (50 tokens)                        │                   │
│  │    "你是澪号。短句。逗号拼接。不叠甲。不感叹。"      │                   │
│  │                   │                                │                   │
│  │                   ▼                                │                   │
│  │  Slot 7: 修正槽 (初始为空)                           │                   │
│  │                   │                                │                   │
│  │                   ▼                                │                   │
│  │  Slot 8: 用户消息                                   │                   │
│  │    "读一下 main.cpp 看看渲染循环问题"                │                   │
│  │                                                    │                   │
│  └──────────────────┬───────────────────────────────┘                   │
│                     │                                                   │
│  返回完整 Prompt → TypeScript → Pi 接管                                 │
│                     │                                                   │
└─────────────────────┼───────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  ④ Pi 继续组装 → DeepSeek API                                           │
│                                                                         │
│  Pi Agent Loop                                                          │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  最终发送给 DeepSeek 的 messages:                   │                   │
│  │                                                    │                   │
│  │  [SYSTEM] Slot 0: 元指令                            │                   │
│  │  [SYSTEM] Slot 1: 身份层 (soul + boundaries)        │                   │
│  │  [SYSTEM] Slot 2: 知识层 (knowledge)                │                   │
│  │  [SYSTEM] Slot 3: [运行环境] Pi原生提示词 + 工具定义  │← Pi原生在此│
│  │  [SYSTEM] Slot 4: 记忆层 (如有)                     │                   │
│  │  ... Chat History ...                               │                   │
│  │  [SYSTEM] Slot 6: 语言锚 (最靠近输出)                │                   │
│  │  [SYSTEM] Slot 7: 修正槽 (如有)                     │                   │
│  │  [USER]  "读一下 main.cpp 看看渲染循环问题"           │                   │
│  │                                                    │                   │
│  │  Pi 原生提示词不再是独立 SYSTEM 块                     │                   │
│  │  → 退化为 Slot 3 内的 [运行环境] 标签                  │                   │
│  │  → 不再与澪号身份竞争 Prompt 权重                      │                   │
│  │                                                    │                   │
│  │  HTTP POST https://api.deepseek.com/v1/chat/...     │                   │
│  │  ─────────────────────────────────────►             │                   │
│  └──────────────────┬───────────────────────────────┘                   │
│                     │                                                   │
│                     │ DeepSeek 返回                                     │
│                     ▼                                                   │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  DeepSeek 模型内部推理:                             │                   │
│  │                                                    │                   │
│  │  看到的上下文顺序:                                   │                   │
│  │    → 元指令: "工具不定义你"                          │                   │
│  │    → 身份: "你是澪号..."                            │                   │
│  │    → 知识: "你懂渲染管线..."                         │                   │
│  │    → 工具: "你可以使用 read, write..."               │                   │
│  │    → 对话历史 (空)                                  │                   │
│  │    → 语言锚: "短句。逗号拼接。不叠甲。"  ← 最靠近输出  │                   │
│  │    → 用户: "读一下 main.cpp 看看渲染循环问题"         │                   │
│  │                                                    │                   │
│  │  生成: "行。看看。"                                  │                   │
│  │  + tool_call: read("D:/project/main.cpp")          │                   │
│  └──────────────────┬───────────────────────────────┘                   │
│                     │                                                   │
│  返回 Pi → 执行工具调用                                                  │
│                     │                                                   │
└─────────────────────┼───────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  ⑤ Pi 执行工具 → 工具返回 → 再次组装                                      │
│                                                                         │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  Pi 执行: read("D:/project/main.cpp")             │                   │
│  │  → 文件内容 3000 字                                │                   │
│  │  → 追加到 chat_history:                            │                   │
│  │    [TOOL_RESULT] "void render() { present(swap..." │                   │
│  └──────────────────┬───────────────────────────────┘                   │
│                     │                                                   │
│                     ▼                                                   │
│  Pi 自动触发下一轮 (用户不需要再发消息)                                    │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  再次调用 pa-mio → harness.build_prompt()          │                   │
│  │                                                    │                   │
│  │  这次 chat_history 变了:                            │                   │
│  │    最后一条是 TOOL_RESULT (3000 字)                  │                   │
│  │    → _last_tool_long() = True                      │                   │
│  │    → 在 chat_history 前插入语言锚                    │                   │
│  │                                                    │                   │
│  │  注入位置变化:                                       │                   │
│  │    ... identity, knowledge, tools ...              │                   │
│  │    [语言锚] ← 额外插入！                            │                   │
│  │    [TOOL_RESULT] void render() { ... 3000字 ...     │                   │
│  │    [语言锚] ← 原来的位置                             │                   │
│  │    [USER] (Pi 自动追加的上下文)                      │                   │
│  └──────────────────┬───────────────────────────────┘                   │
│                     │                                                   │
│                     ▼                                                   │
│  DeepSeek 返回:                                                         │
│  "这个渲染循环的 present 调用放在 swap 之前了。换一下顺序就行。（）"       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  ⑥ 计数器检查 + 输出                                                     │
│                                                                         │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  Pi 接收澪号的回复                                  │                   │
│  │  → 触发 afterMessage 事件                           │                   │
│  │  → pa-mio 调用: bridge.py check {response}         │                   │
│  │                                                    │                   │
│  │  counters.py: check_all(response, chat_history)     │                   │
│  │  ┌────────────────────────────────────┐            │                   │
│  │  │ ✓ 叠甲: 0 个叠甲词                  │            │                   │
│  │  │ ✓ emoji: 0 个                      │            │                   │
│  │  │ ✓ 感叹号: 0 个                     │            │                   │
│  │  │ ✓ 话痨: 长度 72 字                 │            │                   │
│  │  │ ✓ 亲密溢出: 正常                   │            │                   │
│  │  │ → 全绿 → 返回 None                 │            │                   │
│  │  └────────────────────────────────────┘            │                   │
│  │                                                    │                   │
│  └──────────────────┬───────────────────────────────┘                   │
│                     │                                                   │
│                     ▼                                                   │
│  wgnr-pi Web UI                                                         │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  澪号: "这个渲染循环的 present 调用放在 swap         │                   │
│  │         之前了。换一下顺序就行。（）"                │                   │
│  └──────────────────────────────────────────────────┘                   │
│                                                                         │
│  Mirror 看到回复。对话继续...                                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                      │
                      │ (对话进行中，计数器可能在后续轮次触发)
                      │ (如果触发 → 修正槽注入 → 重新生成 → 最多 3 次)
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  ⑦ 对话结束 → 记忆提取                                                   │
│                                                                         │
│  Mirror 关闭对话 / Pi 会话结束时                                         │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  Pi 触发 sessionEnd 事件                            │                   │
│  │  → pa-mio 调用: bridge.py extract                  │                   │
│  │  → stdin: 完整对话全文                               │                   │
│  └──────────────────┬───────────────────────────────┘                   │
│                     │                                                   │
│                     ▼                                                   │
│  extractor.py: extract_memories()                                       │
│  ┌──────────────────────────────────────────────────┐                   │
│  │  独立 API 调用 DeepSeek (不经过澪号人格):            │                   │
│  │                                                    │                   │
│  │  [SYSTEM] "从对话中提取值得长期保存的信息..."        │                   │
│  │  [USER] 《完整对话全文》                             │                   │
│  │                                                    │                   │
│  │  DeepSeek 返回:                                     │                   │
│  │  [{"content": "Mirror 的渲染循环中 present 调用     │                   │
│  │               放在 swap 之前导致同步问题",           │                   │
│  │    "category": "technical",                        │                   │
│  │    "importance": 3}]                               │                   │
│  │                      │                             │                   │
│  │                      ▼                             │                   │
│  │  memory.py: insert_memories()  →  SQLite            │                   │
│  │                                                    │                   │
│  │  INSERT INTO mio_memories (content, category,       │                   │
│  │    importance) VALUES ('Mirror 的渲染循环...',      │                   │
│  │    'technical', 3)                                 │                   │
│  │                                                    │                   │
│  │  下次对话聊到"渲染"时，这条记忆会被检索并注入         │                   │
│  └──────────────────────────────────────────────────┘                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════════════╗
║                        架构中的信息节点汇总                                ║
╠══════════════════════════════════════════════════════════════════════════╣
║                                                                          ║
║  节点              类型           数据方向      内容                      ║
║  ────              ────           ────────      ────                      ║
║  wgnr-pi UI        Web前端        → Pi          用户输入                  ║
║  Pi Agent Loop     Node进程       ↔ 所有        消息路由 + 事件分发        ║
║  pa-mio/index.ts   Pi扩展         ↔ Python      桥接: spawnSync           ║
║  bridge.py         Python CLI     ↔ harness     命令路由                  ║
║  harness.py        Python类       → DeepSeek    Prompt组装                ║
║  counters.py       Python模块     → harness     修正指令或None            ║
║  memory.py         Python模块     → SQLite      记忆存取                  ║
║  extractor.py      Python模块     → DeepSeek    独立API调用(记忆提取)      ║
║  4个.md文件        磁盘文件       → harness     身份/边界/语言/知识         ║
║  SQLite agent.db   磁盘DB         ↔ memory      记忆持久化                ║
║  DeepSeek API      外部API       ← harness      模型推理                  ║
║                                                                          ║
║  关键数据流:                                                              ║
║  用户 → wgnr-pi → Pi → pa-mio → bridge → harness                        ║
║       → DeepSeek → harness(检查) → Pi → wgnr-pi → 用户                  ║
║                                                                          ║
║  独立数据流 (记忆提取):                                                    ║
║  Pi(sessionEnd) → pa-mio → bridge → extractor                           ║
║       → DeepSeek(独立调用) → JSON → memory → SQLite                      ║
║                                                                          ║
╚══════════════════════════════════════════════════════════════════════════╝


╔══════════════════════════════════════════════════════════════════════════╗
║                     一次完整对话中的 Prompt 变化                            ║
╠══════════════════════════════════════════════════════════════════════════╣
║                                                                          ║
║  【第 1 轮】 用户: "读一下 main.cpp"                                       ║
║                                                                          ║
║  Prompt = meta + soul + boundaries + knowledge + tools                   ║
║         + (无记忆) + (无历史) + 语言锚 + 修正槽(空) + 用户消息              ║
║                                                                          ║
║  澪号 → tool_call: read("main.cpp")                                     ║
║                                                                          ║
║  ————————————————————————————————————————————————————————————————————    ║
║                                                                          ║
║  【第 2 轮】 工具返回 3000 字 (Pi 自动触发)                                 ║
║                                                                          ║
║  Prompt = meta + soul + boundaries + knowledge + tools                   ║
║         + (无记忆)                                                        ║
║         + 语言锚(额外!) ← 对抗 3000 字工具输出                             ║
║         + [TOOL_RESULT] 3000 字                                           ║
║         + 语言锚(正常位置)                                                 ║
║         + 修正槽(空) + (Pi上下文)                                          ║
║                                                                          ║
║  澪号 → "换一下顺序就行。（）"  ← 语言锚生效，风格完好                      ║
║                                                                          ║
║  ————————————————————————————————————————————————————————————————————    ║
║                                                                          ║
║  【计数器触发的情况】 澪号: "哈哈哈！！！太对了 😂😂😂"                        ║
║                                                                          ║
║  第 1 次 → counters 触发 "emoji溢出" + "感叹号溢出"                        ║
║          → 修正槽注入 → 重新生成                                           ║
║  第 2 次 → 修正后输出: "hhh，确实（）" ← 通过                               ║
║                                                                          ║
║  Mirror 看到的是第 2 次的结果，对修复过程无感                                ║
║                                                                          ║
╚══════════════════════════════════════════════════════════════════════════╝
"""

print(FLOW)
