# MEMORY — 环境与项目记忆

§ Mirror 偏好 TypeScript 严格模式，不接受 any 类型绕过
§ 项目 personal-agent 使用 SolidJS + Tailwind，桥接层是 Node.js + Pi SDK + WebSocket
§ Dashboard 的设置系统 Phase 1-2 已完成，模型切换移到了设置页面
§ Mirror 写代码前会先读 CLAUDE.md 了解项目规范
§ 前端扩展通过 registry 注册，每个扩展独立文件夹，不互相 import
§ Bridge 通过 Pi Agent SDK 管理 AI 会话，WebSocket 推流到前端
