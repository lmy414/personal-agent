# Live2D MCP 通用组件 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Live2D 控制逻辑抽离为独立的 MCP CLI 工具，支持 Claude Desktop/OpenClaw 等通过 STDIO 调用表情/动作/动画控制。

**Architecture:** 单进程 Node.js 应用。STDIN/STDOUT 承载 MCP JSON-RPC 协议，内部 WebSocket（127.0.0.1）广播指令给渲染器。模型文件由用户配置路径指定，表情列表从 model3.json 动态读取。

**Tech Stack:** TypeScript, Node.js, `ws`, `tsx` (dev runtime)

---

## 文件结构

```
D:\claude\live2d-mcp\              ← 独立仓库
├── package.json
├── tsconfig.json
├── .gitignore
├── src/
│   ├── types.ts                   ← 所有共享类型定义
│   ├── config.ts                  ← ~/.live2d-mcp/config.json 读写 + 校验
│   ├── model-reader.ts            ← 解析 model3.json → ModelManifest
│   ├── expressions.ts             ← 表情注册表（模型表情 + 内置 smile/bigsmile/sad）
│   ├── animations.ts              ← 语义动画定义（wink/nod 等 → 参数序列）
│   ├── ws-hub.ts                  ← 内部 WebSocket 广播 hub
│   ├── tools.ts                   ← 5 个 MCP 工具定义 + dispatch
│   ├── server.ts                  ← MCP STDIO 传输 + JSON-RPC 路由
│   └── cli.ts                     ← CLI 入口（init / start）
├── test/
│   └── test.ts                    ← 集成测试（启动 server，模拟 STDIO 调用）
└── README.md
```

---

### Task 1: 项目脚手架

**Files:**
- Create: `D:\claude\live2d-mcp\package.json`
- Create: `D:\claude\live2d-mcp\tsconfig.json`
- Create: `D:\claude\live2d-mcp\.gitignore`

- [ ] **Step 1: 创建 package.json**

```json
{
  "name": "live2d-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "live2d-mcp": "./dist/cli.js"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc",
    "start": "node dist/cli.js",
    "test": "tsx test/test.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
```

- [ ] **Step 2: 创建 tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "declaration": true
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist", "test"]
}
```

- [ ] **Step 3: 创建 .gitignore**

```
node_modules/
dist/
*.log
```

- [ ] **Step 4: 安装依赖并验证编译**

```bash
cd D:\claude\live2d-mcp && npm install && npx tsc --noEmit
```

Expected: 因为还没有 .ts 文件，`tsc --noEmit` 应无错误（或只报 "No inputs were found"）

- [ ] **Step 5: 初始化 Git 并提交**

```bash
cd D:\claude\live2d-mcp && git init && git add -A && git commit -m "chore: 项目脚手架"
```

---

### Task 2: 共享类型定义

**Files:**
- Create: `D:\claude\live2d-mcp\src\types.ts`

- [ ] **Step 1: 写入所有共享类型**

```typescript
// ═══════════════════════════════════════
// 配置
// ═══════════════════════════════════════

export interface Live2DMCPConfig {
  model: { path: string }
  ws: { port: number; host: string }
}

// ═══════════════════════════════════════
// 模型元数据（从 model3.json 解析）
// ═══════════════════════════════════════

export interface ExpressionRef {
  name: string   // 文件名去 .exp3.json 后缀
  file: string   // 相对路径（相对于 model3.json 所在目录）
}

export interface MotionGroupRef {
  name: string
  files: string[]
}

export interface ModelManifest {
  name: string
  expressions: ExpressionRef[]
  motions: MotionGroupRef[]
  rawPath: string    // model3.json 绝对路径
  baseDir: string    // model3.json 父目录
}

// ═══════════════════════════════════════
// 渲染指令
// ═══════════════════════════════════════

export interface ParameterCommand {
  id: string
  value: number
  duration?: number   // ms，默认 0（立即）
  easing?: string     // linear / easeIn / easeOut / easeInOut
}

export interface WSBroadcast {
  type: 'expression' | 'motion' | 'parameter' | 'animate'
  name: string
  params: ParameterCommand[]
  motionFile?: string   // 仅 motion 类型
}

// ═══════════════════════════════════════
// MCP JSON-RPC
// ═══════════════════════════════════════

export interface JsonRpcRequest {
  jsonrpc: '2.0'
  id?: number | string
  method: string
  params?: Record<string, unknown>
}

export interface MCPToolDef {
  name: string
  description: string
  inputSchema: {
    type: 'object'
    properties: Record<string, unknown>
    required?: string[]
  }
}

export interface MCPCallResult {
  content: Array<{ type: 'text'; text: string }>
}
```

- [ ] **Step 2: 验证编译**

```bash
cd D:\claude\live2d-mcp && npx tsc --noEmit
```
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "feat: 共享类型定义"
```

---

### Task 3: 配置模块

**Files:**
- Create: `D:\claude\live2d-mcp\src\config.ts`

- [ ] **Step 1: 实现 config.ts**

```typescript
import * as fs from 'node:fs'
import * as path from 'node:path'
import * as os from 'node:os'
import * as readline from 'node:readline'
import type { Live2DMCPConfig } from './types.js'

const CONFIG_DIR = path.join(os.homedir(), '.live2d-mcp')
const CONFIG_PATH = path.join(CONFIG_DIR, 'config.json')

const DEFAULTS: Partial<Live2DMCPConfig> = {
  ws: { port: 9228, host: '127.0.0.1' },
}

export function configPath(): string {
  return CONFIG_PATH
}

export function readConfig(): Live2DMCPConfig {
  if (!fs.existsSync(CONFIG_PATH)) {
    throw new Error(
      `配置文件不存在: ${CONFIG_PATH}\n请先运行 live2d-mcp init 创建配置`,
    )
  }
  const raw = fs.readFileSync(CONFIG_PATH, 'utf-8')
  const parsed = JSON.parse(raw) as Partial<Live2DMCPConfig>

  if (!parsed.model?.path || typeof parsed.model.path !== 'string') {
    throw new Error('配置文件缺少 model.path 字段')
  }
  if (!fs.existsSync(parsed.model.path)) {
    throw new Error(`模型文件不存在: ${parsed.model.path}`)
  }

  const wsPort = parsed.ws?.port ?? DEFAULTS.ws!.port
  const wsHost = parsed.ws?.host ?? DEFAULTS.ws!.host

  return {
    model: { path: parsed.model.path },
    ws: { port: wsPort, host: wsHost },
  }
}

export async function initConfig(): Promise<void> {
  const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout,
  })

  const question = (q: string): Promise<string> =>
    new Promise((resolve) => rl.question(q, resolve))

  console.log('Live2D MCP — 首次配置\n')

  const modelPath = await question('请输入 model3.json 的绝对路径: ')
  const resolved = path.resolve(modelPath.trim())

  if (!fs.existsSync(resolved)) {
    console.error(`错误: 文件不存在 — ${resolved}`)
    rl.close()
    process.exit(1)
  }

  if (!resolved.endsWith('.model3.json')) {
    console.warn('警告: 文件名不以 .model3.json 结尾，请确认这是有效的 Cubism 模型文件')
  }

  const config: Live2DMCPConfig = {
    model: { path: resolved },
    ws: { port: 9228, host: '127.0.0.1' },
  }

  fs.mkdirSync(CONFIG_DIR, { recursive: true })
  fs.writeFileSync(CONFIG_PATH, JSON.stringify(config, null, 2) + '\n', 'utf-8')

  console.log(`\n配置已写入: ${CONFIG_PATH}`)
  console.log(`模型路径: ${resolved}`)
  console.log(`\n现在可以运行 live2d-mcp start 启动服务`)

  rl.close()
}
```

- [ ] **Step 2: 验证编译**

```bash
cd D:\claude\live2d-mcp && npx tsc --noEmit
```
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "feat: 配置模块 — 读写 ~/.live2d-mcp/config.json"
```

---

### Task 4: 模型读取器

**Files:**
- Create: `D:\claude\live2d-mcp\src\model-reader.ts`

- [ ] **Step 1: 实现 model-reader.ts**

```typescript
import * as fs from 'node:fs'
import * as path from 'node:path'
import type { ModelManifest, ExpressionRef, MotionGroupRef } from './types.js'

interface CubismModel3Json {
  Version: number
  Name?: string
  FileReferences: {
    Moc: string
    Textures: string[]
    Physics?: string
    Pose?: string
    Expressions?: Array<{ Name: string; File: string }>
    Motions?: Record<string, Array<{ File: string; Sound?: string; Text?: string }>>
    [key: string]: unknown
  }
  Groups?: Array<{ Name: string; Ids: string[] }>
  HitAreas?: unknown[]
}

export function readModelManifest(modelPath: string): ModelManifest {
  const raw = fs.readFileSync(modelPath, 'utf-8')
  const json = JSON.parse(raw) as CubismModel3Json

  if (!json.FileReferences?.Moc) {
    throw new Error(`无效的 model3.json: 缺少 FileReferences.Moc — ${modelPath}`)
  }

  const baseDir = path.dirname(modelPath)
  const name = json.Name ?? path.basename(baseDir)

  // 解析表情列表
  const expressions: ExpressionRef[] = (json.FileReferences.Expressions ?? []).map(
    (e) => ({
      name: e.Name,
      file: e.File,
    }),
  )

  // 解析动作组列表
  const motions: MotionGroupRef[] = []
  if (json.FileReferences.Motions) {
    for (const [groupName, entries] of Object.entries(json.FileReferences.Motions)) {
      motions.push({
        name: groupName,
        files: entries.map((e) => e.File),
      })
    }
  }

  return { name, expressions, motions, rawPath: modelPath, baseDir }
}
```

- [ ] **Step 2: 验证编译**

```bash
cd D:\claude\live2d-mcp && npx tsc --noEmit
```
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "feat: 模型读取器 — 解析 model3.json 提取表情/动作列表"
```

---

### Task 5: 表情注册表

**Files:**
- Create: `D:\claude\live2d-mcp\src\expressions.ts`

- [ ] **Step 1: 实现 expressions.ts**

```typescript
import * as fs from 'node:fs'
import * as path from 'node:path'
import type { ModelManifest, ParameterCommand } from './types.js'

// ═══════════════════════════════════════
// 内置参数表情（不依赖模型 .exp3.json）
// ═══════════════════════════════════════

const BUILTIN: Record<string, ParameterCommand[]> = {
  smile: [
    { id: 'ParamEyeLSmile', value: 0.6 },
    { id: 'ParamEyeRSmile', value: 0.6 },
    { id: 'ParamMouthForm', value: 0.5 },
  ],
  bigsmile: [
    { id: 'ParamEyeLSmile', value: 1.0 },
    { id: 'ParamEyeRSmile', value: 1.0 },
    { id: 'ParamMouthForm', value: 0.8 },
    { id: 'ParamCheek', value: 0.5 },
    { id: 'ParamMouthOpenY', value: 0.3 },
  ],
  sad: [
    { id: 'ParamBrowLY', value: 0.4 },
    { id: 'ParamBrowRY', value: 0.4 },
    { id: 'ParamBrowLForm', value: -0.4 },
    { id: 'ParamBrowRForm', value: -0.4 },
    { id: 'ParamMouthForm', value: -0.3 },
  ],
  kongbai: [], // 空白 = 清除所有参数表情
}

// ═══════════════════════════════════════
// 表达式名列表
// ═══════════════════════════════════════

export interface ExpressionEntry {
  name: string
  source: 'builtin' | 'model' | 'animation'
  description: string
}

const BUILTIN_DESC: Record<string, string> = {
  smile: '微笑 — 嘴角上扬',
  bigsmile: '大笑 — 张嘴笑',
  sad: '悲伤 — 眉毛下垂',
  kongbai: '空白 — 清除表情恢复默认',
}

export function listExpressions(manifest: ModelManifest): ExpressionEntry[] {
  const entries: ExpressionEntry[] = []

  // 内置参数表情
  for (const name of Object.keys(BUILTIN)) {
    entries.push({ name, source: 'builtin', description: BUILTIN_DESC[name] ?? '' })
  }

  // 模型表达式
  for (const expr of manifest.expressions) {
    if (entries.some((e) => e.name === expr.name)) continue // 内置优先
    entries.push({ name: expr.name, source: 'model', description: '' })
  }

  return entries
}

// ═══════════════════════════════════════
// 解析表达式 → 参数数组
// ═══════════════════════════════════════

export function resolveExpression(
  manifest: ModelManifest,
  name: string,
): ParameterCommand[] | null {
  // 1. 检查内置表情
  if (name in BUILTIN) {
    return BUILTIN[name]
  }

  // 2. 检查模型表情
  const exprRef = manifest.expressions.find((e) => e.name === name)
  if (!exprRef) return null

  const exprPath = path.resolve(manifest.baseDir, exprRef.file)
  if (!fs.existsSync(exprPath)) {
    // 尝试从 model3.json 同级目录解析
    // 有些模型的 Expression.File 是相对 model3.json 的路径
    return null
  }

  try {
    const raw = fs.readFileSync(exprPath, 'utf-8')
    const json = JSON.parse(raw) as { Parameters?: Array<{ Id: string; Value: number; Blend?: string }> }
    if (!json.Parameters) return []
    return json.Parameters.map((p) => ({
      id: p.Id,
      value: p.Value,
    }))
  } catch {
    return null
  }
}
```

- [ ] **Step 2: 验证编译**

```bash
cd D:\claude\live2d-mcp && npx tsc --noEmit
```
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "feat: 表情注册表 — 内置参数表情 + 模型表情读取"
```

---

### Task 6: 语义动画

**Files:**
- Create: `D:\claude\live2d-mcp\src\animations.ts`

- [ ] **Step 1: 实现 animations.ts**

```typescript
import type { ParameterCommand } from './types.js'

export interface AnimationDef {
  name: string
  description: string
  params: ParameterCommand[]   // 按顺序执行，每个带 duration
}

const ANIMATIONS: Record<string, AnimationDef> = {
  wink: {
    name: 'wink',
    description: '单眼眨 — 只眨左眼',
    params: [
      { id: 'ParamEyeLOpen', value: 0, duration: 120, easing: 'easeInOut' },
      { id: 'ParamEyeLOpen', value: 1, duration: 120, easing: 'easeInOut' },
    ],
  },
  slow_blink: {
    name: 'slow_blink',
    description: '慢眨眼 — 双眼缓慢闭合再睁开',
    params: [
      { id: 'ParamEyeLOpen', value: 0, duration: 400, easing: 'easeInOut' },
      { id: 'ParamEyeROpen', value: 0, duration: 400, easing: 'easeInOut' },
      { id: 'ParamEyeLOpen', value: 1, duration: 400, easing: 'easeInOut' },
      { id: 'ParamEyeROpen', value: 1, duration: 400, easing: 'easeInOut' },
    ],
  },
  double_blink: {
    name: 'double_blink',
    description: '双眨眼 — 快速连眨两次',
    params: [
      { id: 'ParamEyeLOpen', value: 0, duration: 80, easing: 'easeInOut' },
      { id: 'ParamEyeROpen', value: 0, duration: 80, easing: 'easeInOut' },
      { id: 'ParamEyeLOpen', value: 1, duration: 80, easing: 'easeInOut' },
      { id: 'ParamEyeROpen', value: 1, duration: 80, easing: 'easeInOut' },
      { id: 'ParamEyeLOpen', value: 0, duration: 80, easing: 'easeInOut' },
      { id: 'ParamEyeROpen', value: 0, duration: 80, easing: 'easeInOut' },
      { id: 'ParamEyeLOpen', value: 1, duration: 80, easing: 'easeInOut' },
      { id: 'ParamEyeROpen', value: 1, duration: 80, easing: 'easeInOut' },
    ],
  },
  nod: {
    name: 'nod',
    description: '点头 — 上下点头',
    params: [
      { id: 'ParamAngleX', value: -15, duration: 250, easing: 'easeInOut' },
      { id: 'ParamAngleX', value: 0, duration: 250, easing: 'easeInOut' },
      { id: 'ParamAngleX', value: -10, duration: 200, easing: 'easeInOut' },
      { id: 'ParamAngleX', value: 0, duration: 200, easing: 'easeInOut' },
    ],
  },
  shake_head: {
    name: 'shake_head',
    description: '摇头 — 左右摇头',
    params: [
      { id: 'ParamAngleY', value: -15, duration: 200, easing: 'easeInOut' },
      { id: 'ParamAngleY', value: 15, duration: 200, easing: 'easeInOut' },
      { id: 'ParamAngleY', value: -10, duration: 200, easing: 'easeInOut' },
      { id: 'ParamAngleY', value: 0, duration: 150, easing: 'easeInOut' },
    ],
  },
  tilt_head: {
    name: 'tilt_head',
    description: '歪头 — 头向一侧倾斜',
    params: [
      { id: 'ParamAngleZ', value: -20, duration: 300, easing: 'easeInOut' },
      { id: 'ParamAngleZ', value: 0, duration: 300, easing: 'easeInOut' },
    ],
  },
}

export function listAnimations(): AnimationDef[] {
  return Object.values(ANIMATIONS)
}

export function resolveAnimation(name: string): AnimationDef | null {
  return ANIMATIONS[name] ?? null
}

export const ANIMATION_NAMES = Object.keys(ANIMATIONS)
```

- [ ] **Step 2: 验证编译**

```bash
cd D:\claude\live2d-mcp && npx tsc --noEmit
```
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "feat: 语义动画 — wink/nod/shake 等参数序列"
```

---

### Task 7: WebSocket Hub

**Files:**
- Create: `D:\claude\live2d-mcp\src\ws-hub.ts`

- [ ] **Step 1: 实现 ws-hub.ts**

```typescript
import { WebSocketServer, WebSocket } from 'ws'
import type { WSBroadcast } from './types.js'

export interface WSHub {
  broadcast(msg: WSBroadcast): void
  close(): void
}

export function startWSHub(port: number, host: string): WSHub {
  const wss = new WebSocketServer({ port, host })
  const clients = new Set<WebSocket>()

  wss.on('connection', (ws) => {
    clients.add(ws)
    ws.on('close', () => clients.delete(ws))
    ws.on('error', () => clients.delete(ws))
  })

  wss.on('error', (err) => {
    console.error(`[live2d-mcp] WS server error: ${(err as Error).message}`)
  })

  console.error(`[live2d-mcp] WS hub listening on ws://${host}:${port}`)

  return {
    broadcast(msg: WSBroadcast) {
      const raw = JSON.stringify(msg)
      for (const ws of clients) {
        if (ws.readyState === WebSocket.OPEN) {
          ws.send(raw)
        }
      }
    },
    close() {
      wss.close()
    },
  }
}
```

- [ ] **Step 2: 验证编译**

```bash
cd D:\claude\live2d-mcp && npx tsc --noEmit
```
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "feat: WebSocket hub — 渲染器广播通道"
```

---

### Task 8: MCP 工具定义 + 调度

**Files:**
- Create: `D:\claude\live2d-mcp\src\tools.ts`

- [ ] **Step 1: 实现 tools.ts**

```typescript
import type { ModelManifest, MCPToolDef, MCPCallResult, ParameterCommand } from './types.js'
import type { WSHub } from './ws-hub.js'
import { listExpressions, resolveExpression } from './expressions.js'
import { listAnimations, resolveAnimation } from './animations.js'
import * as path from 'node:path'

function listMotionNames(manifest: ModelManifest): string[] {
  return manifest.motions.map((m) => m.name)
}

export function createTools(manifest: ModelManifest, ws?: WSHub): MCPToolDef[] {
  const exprNames = listExpressions(manifest).map((e) => e.name)
  const motionNames = listMotionNames(manifest)
  const animNames = listAnimations().map((a) => a.name)

  return [
    {
      name: 'live2d_expression',
      description:
        '切换 Live2D 表情。根据当前情绪选择合适表情。' +
        '可用: ' + exprNames.join(', '),
      inputSchema: {
        type: 'object',
        properties: {
          name: {
            type: 'string',
            description: '表情名称。可选: ' + exprNames.join(', '),
          },
        },
        required: ['name'],
      },
    },
    {
      name: 'live2d_motion',
      description:
        '播放 Live2D 动作组。' +
        (motionNames.length > 0
          ? '可用: ' + motionNames.join(', ')
          : '当前模型无可用动作组'),
      inputSchema: {
        type: 'object',
        properties: {
          name: { type: 'string', description: '动作组名称。可选: ' + motionNames.join(', ') },
        },
        required: ['name'],
      },
    },
    {
      name: 'live2d_status',
      description: '获取 Live2D 状态：模型名称、可用表情、动作、动画列表',
      inputSchema: { type: 'object', properties: {} },
    },
    {
      name: 'live2d_parameter',
      description:
        '直接操控 Live2D 底层参数。常用: ParamEyeLOpen(0=闭,1=开)、ParamAngleX(上下)、ParamAngleY(左右)、ParamAngleZ(歪头)、ParamBreath(呼吸)、ParamMouthOpenY(张嘴)。通过 live2d_status 获取更多。',
      inputSchema: {
        type: 'object',
        properties: {
          params: {
            type: 'array',
            items: {
              type: 'object',
              properties: {
                id: { type: 'string', description: '参数 ID' },
                value: { type: 'number', description: '目标值' },
                duration: { type: 'number', description: '过渡时长(ms)，默认 0' },
                easing: { type: 'string', description: '缓动: linear/easeIn/easeOut/easeInOut' },
              },
              required: ['id', 'value'],
            },
          },
        },
        required: ['params'],
      },
    },
    {
      name: 'live2d_animate',
      description:
        '播放语义动画。可用: ' + listAnimations().map((a) => `${a.name}(${a.description})`).join(', '),
      inputSchema: {
        type: 'object',
        properties: {
          name: {
            type: 'string',
            description: '动画名称。可选: ' + animNames.join(', '),
          },
        },
        required: ['name'],
      },
    },
  ]
}

export function dispatchTool(
  toolName: string,
  args: Record<string, unknown>,
  manifest: ModelManifest,
  ws?: WSHub,
): MCPCallResult {
  switch (toolName) {
    case 'live2d_status': {
      const exprList = listExpressions(manifest)
      const animList = listAnimations()
      const motionList = listMotionNames(manifest)
      const text = [
        `模型: ${manifest.name}`,
        `模型路径: ${manifest.rawPath}`,
        `表情(${exprList.length}): ${exprList.map((e) => `${e.name}(${e.description || e.source})`).join(', ')}`,
        `动作组(${motionList.length}): ${motionList.join(', ') || '(无)'}`,
        `动画(${animList.length}): ${animList.map((a) => `${a.name}(${a.description})`).join(', ')}`,
      ].join('\n')
      return { content: [{ type: 'text', text }] }
    }

    case 'live2d_expression': {
      const name = args.name as string
      if (!name || typeof name !== 'string') {
        return { content: [{ type: 'text', text: '错误: 缺少表情名称参数' }] }
      }
      const params = resolveExpression(manifest, name)
      if (params === null) {
        const available = listExpressions(manifest).map((e) => e.name).join(', ')
        return { content: [{ type: 'text', text: `未知表情: ${name}。可用: ${available}` }] }
      }
      ws?.broadcast({ type: 'expression', name, params })
      const entry = listExpressions(manifest).find((e) => e.name === name)
      return { content: [{ type: 'text', text: `表情已切换: ${name}${entry?.description ? ' (' + entry.description + ')' : ''}` }] }
    }

    case 'live2d_motion': {
      const name = args.name as string
      if (!name || typeof name !== 'string') {
        return { content: [{ type: 'text', text: '错误: 缺少动作名称参数' }] }
      }
      const motion = manifest.motions.find((m) => m.name === name)
      if (!motion) {
        const available = listMotionNames(manifest).join(', ') || '(无)'
        return { content: [{ type: 'text', text: `未知动作组: ${name}。可用: ${available}` }] }
      }
      // 广播第一个 motion 文件路径
      const motionFile = motion.files[0]
        ? path.resolve(manifest.baseDir, motion.files[0])
        : undefined
      ws?.broadcast({ type: 'motion', name, params: [], motionFile })
      return { content: [{ type: 'text', text: `动作已播放: ${name}` }] }
    }

    case 'live2d_parameter': {
      const params = args.params as ParameterCommand[] | undefined
      if (!params || !Array.isArray(params) || params.length === 0) {
        return { content: [{ type: 'text', text: '错误: 缺少 params 参数数组' }] }
      }
      ws?.broadcast({ type: 'parameter', name: 'custom', params })
      return { content: [{ type: 'text', text: `参数已应用 (${params.length} 项)` }] }
    }

    case 'live2d_animate': {
      const name = args.name as string
      if (!name || typeof name !== 'string') {
        return { content: [{ type: 'text', text: '错误: 缺少动画名称参数' }] }
      }
      const anim = resolveAnimation(name)
      if (!anim) {
        const available = listAnimations().map((a) => a.name).join(', ')
        return { content: [{ type: 'text', text: `未知动画: ${name}。可用: ${available}` }] }
      }
      ws?.broadcast({ type: 'animate', name, params: anim.params })
      return { content: [{ type: 'text', text: `动画已播放: ${name} (${anim.description})` }] }
    }

    default:
      return { content: [{ type: 'text', text: `未知工具: ${toolName}` }] }
  }
}
```

- [ ] **Step 2: 验证编译**

```bash
cd D:\claude\live2d-mcp && npx tsc --noEmit
```
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "feat: MCP 工具定义 + 调度 — 5 个工具"
```

---

### Task 9: MCP Server（STDIO 传输）

**Files:**
- Create: `D:\claude\live2d-mcp\src\server.ts`

- [ ] **Step 1: 实现 server.ts**

```typescript
import { createInterface } from 'node:readline'
import type { JsonRpcRequest } from './types.js'
import type { ModelManifest } from './types.js'
import type { WSHub } from './ws-hub.js'
import { createTools, dispatchTool } from './tools.js'

function ok(id: number | string | undefined, result: unknown): string {
  return JSON.stringify({ jsonrpc: '2.0', id, result })
}

function rpcErr(id: number | string | undefined, code: number, message: string): string {
  return JSON.stringify({ jsonrpc: '2.0', id, error: { code, message } })
}

function log(msg: string): void {
  process.stderr.write(`[live2d-mcp] ${msg}\n`)
}

export function startServer(manifest: ModelManifest, wsHub: WSHub): void {
  const tools = createTools(manifest, wsHub)
  log(`模型: ${manifest.name}`)
  log(`表情: ${manifest.expressions.length} 个模型表情`)
  log(`动作组: ${manifest.motions.length} 个`)
  log(`MCP Server 已启动，等待 STDIO 连接...`)

  const rl = createInterface({ input: process.stdin })

  rl.on('line', (line: string) => {
    let req: JsonRpcRequest
    try {
      req = JSON.parse(line.trim())
    } catch {
      log(`JSON 解析失败: ${line.slice(0, 80)}`)
      return
    }

    const { id, method, params } = req

    try {
      switch (method) {
        case 'initialize':
          process.stdout.write(
            ok(id, {
              protocolVersion: '2024-11-05',
              serverInfo: { name: 'live2d-mcp', version: '0.1.0' },
              capabilities: { tools: {} },
            }) + '\n',
          )
          break

        case 'tools/list':
          process.stdout.write(ok(id, { tools }) + '\n')
          break

        case 'tools/call': {
          const toolName = params?.name as string | undefined
          const toolArgs = (params?.arguments ?? {}) as Record<string, unknown>

          if (!toolName) {
            process.stdout.write(rpcErr(id, -32602, 'Missing tool name') + '\n')
            return
          }

          const result = dispatchTool(toolName, toolArgs, manifest, wsHub)
          process.stdout.write(ok(id, result) + '\n')
          break
        }

        case 'ping':
          process.stdout.write(ok(id, {}) + '\n')
          break

        // 通知类（无 id）- 不响应
        case 'initialized':
        case 'notifications/initialized':
          break

        default:
          process.stdout.write(rpcErr(id, -32601, `Unknown method: ${method}`) + '\n')
      }
    } catch (err) {
      log(`处理 ${method} 时出错: ${(err as Error).message}`)
      process.stdout.write(rpcErr(id, -32603, 'Internal error') + '\n')
    }
  })

  rl.on('close', () => {
    log('stdin closed, exiting')
    wsHub.close()
    process.exit(0)
  })
}
```

- [ ] **Step 2: 验证编译**

```bash
cd D:\claude\live2d-mcp && npx tsc --noEmit
```
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "feat: MCP Server — STDIO 传输 + JSON-RPC 路由"
```

---

### Task 10: CLI 入口

**Files:**
- Create: `D:\claude\live2d-mcp\src\cli.ts`

- [ ] **Step 1: 实现 cli.ts**

```typescript
#!/usr/bin/env node
import { readConfig, initConfig } from './config.js'
import { readModelManifest } from './model-reader.js'
import { startWSHub } from './ws-hub.js'
import { startServer } from './server.js'

const USAGE = `Live2D MCP — 通用 Live2D 控制服务

用法:
  live2d-mcp init     创建配置文件 (~/.live2d-mcp/config.json)
  live2d-mcp start    启动 MCP Server（STDIO 模式）
  live2d-mcp --help   显示帮助
  live2d-mcp version  显示版本
`

async function main(): Promise<void> {
  const cmd = process.argv[2] ?? 'start'

  if (cmd === '--help' || cmd === '-h') {
    console.log(USAGE)
    return
  }

  if (cmd === 'version' || cmd === '--version' || cmd === '-v') {
    // 动态读 package.json
    const pkg = await import('../package.json', { with: { type: 'json' } })
    console.log(pkg.default.version)
    return
  }

  if (cmd === 'init') {
    await initConfig()
    return
  }

  if (cmd === 'start') {
    const config = readConfig()
    const manifest = readModelManifest(config.model.path)
    const wsHub = startWSHub(config.ws.port, config.ws.host)
    startServer(manifest, wsHub)
    return
  }

  console.error(`未知命令: ${cmd}\n${USAGE}`)
  process.exit(1)
}

main().catch((err) => {
  console.error(`[live2d-mcp] 启动失败: ${(err as Error).message}`)
  process.exit(1)
})
```

- [ ] **Step 2: 验证编译**

```bash
cd D:\claude\live2d-mcp && npx tsc --noEmit
```
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "feat: CLI 入口 — init / start 命令"
```

---

### Task 11: 集成测试

**Files:**
- Create: `D:\claude\live2d-mcp\test\test.ts`

- [ ] **Step 1: 实现集成测试**

```typescript
/**
 * 集成测试 — 不启动 WS hub，直接测试 MCP 协议 + 工具调度逻辑
 *
 * 用法: npx tsx test/test.ts
 */
import { readModelManifest } from '../src/model-reader.js'
import { createTools, dispatchTool } from '../src/tools.js'
import { listExpressions } from '../src/expressions.js'
import { listAnimations, resolveAnimation } from '../src/animations.js'
import { readConfig } from '../src/config.js'

let failures = 0
let passed = 0

function assert(condition: boolean, label: string): void {
  if (condition) {
    console.log(`  ✓ ${label}`)
    passed++
  } else {
    console.error(`  ✗ ${label}`)
    failures++
  }
}

async function main(): Promise<void> {
  console.log('=== Live2D MCP 集成测试 ===\n')

  // 读配置
  let config
  try {
    config = readConfig()
    console.log(`配置: OK (模型: ${config.model.path})`)
  } catch (e) {
    console.error(`配置错误: ${(e as Error).message}`)
    console.log('跳过需要模型的测试...')
    // 测试纯代码逻辑
    testAnimations()
    testToolsWithoutManifest()
    printResults()
    return
  }

  // 读模型
  const manifest = readModelManifest(config.model.path)
  console.log(`模型名: ${manifest.name}`)
  console.log(`表情数: ${manifest.expressions.length}`)
  console.log(`动作组: ${manifest.motions.length}`)

  // ── 表情 ──
  console.log('\n── 表情 ──')
  const exprs = listExpressions(manifest)
  assert(exprs.length > 0, `listExpressions 返回 ${exprs.length} 个表情`)
  assert(exprs.some((e) => e.name === 'smile'), '内置表情 smile 存在')
  assert(exprs.some((e) => e.name === 'bigsmile'), '内置表情 bigsmile 存在')
  assert(exprs.some((e) => e.name === 'sad'), '内置表情 sad 存在')
  assert(exprs.some((e) => e.name === 'kongbai'), '内置表情 kongbai 存在')

  // ── 工具定义 ──
  console.log('\n── 工具定义 ──')
  const tools = createTools(manifest)
  assert(tools.length === 5, `createTools 返回 ${tools.length} 个工具`)
  const toolNames = tools.map((t) => t.name)
  assert(toolNames.includes('live2d_expression'), '工具 live2d_expression 已注册')
  assert(toolNames.includes('live2d_motion'), '工具 live2d_motion 已注册')
  assert(toolNames.includes('live2d_status'), '工具 live2d_status 已注册')
  assert(toolNames.includes('live2d_parameter'), '工具 live2d_parameter 已注册')
  assert(toolNames.includes('live2d_animate'), '工具 live2d_animate 已注册')

  // ── 工具调度 ──
  console.log('\n── 工具调度 ──')
  const statusResult = dispatchTool('live2d_status', {}, manifest)
  assert(
    statusResult.content[0].text.includes(manifest.name),
    'live2d_status 包含模型名称',
  )

  const exprResult = dispatchTool('live2d_expression', { name: 'smile' }, manifest)
  assert(
    exprResult.content[0].text.includes('smile'),
    'live2d_expression(smile) 返回成功',
  )

  const badExpr = dispatchTool('live2d_expression', { name: 'nonexistent' }, manifest)
  assert(
    badExpr.content[0].text.includes('未知表情'),
    'live2d_expression(不存在) 返回错误',
  )

  const paramResult = dispatchTool(
    'live2d_parameter',
    { params: [{ id: 'ParamEyeLOpen', value: 0.5 }] },
    manifest,
  )
  assert(
    paramResult.content[0].text.includes('1 项'),
    'live2d_parameter 返回成功',
  )

  // 语义动画
  testAnimations()

  // ── 完整工具调用流程 ──
  console.log('\n── 完整调用流程 ──')
  if (manifest.expressions.length > 0) {
    const first = manifest.expressions[0].name
    const r = dispatchTool('live2d_expression', { name: first }, manifest)
    assert(
      r.content[0].text.includes(first) || r.content[0].text.includes('表情已切换'),
      `模型表情 ${first} 调用成功`,
    )
  }

  printResults()
}

function testAnimations(): void {
  console.log('\n── 语义动画 ──')
  const anims = listAnimations()
  assert(anims.length >= 6, `listAnimations 返回 ${anims.length} 个动画`)
  assert(anims.some((a) => a.name === 'wink'), '动画 wink 存在')
  assert(resolveAnimation('wink')!.params.length >= 2, 'wink 参数数 >= 2')
  assert(resolveAnimation('nod')!.params.length >= 2, 'nod 参数数 >= 2')
  assert(resolveAnimation('nonexistent') === null, '不存在动画返回 null')
}

function testToolsWithoutManifest(): void {
  // 不做模型相关测试
  console.log('\n── 无模型测试跳过 ──')
  testAnimations()
}

function printResults(): void {
  console.log(`\n=== 结果: ${passed} 通过, ${failures} 失败 ===`)
  if (failures > 0) process.exit(1)
}

main()
```

- [ ] **Step 2: 运行测试**

```bash
cd D:\claude\live2d-mcp && npx tsx test/test.ts
```

Expected: 所有测试通过，或有配置提示。如果 `~/.live2d-mcp/config.json` 不存在，测试会跳过需要模型的部分但仍验证动画逻辑。

- [ ] **Step 3: 提交**

```bash
git add -A && git commit -m "test: 集成测试 — MCP 协议 + 工具调度"
```

---

### Task 12: README

**Files:**
- Create: `D:\claude\live2d-mcp\README.md`

- [ ] **Step 1: 编写 README**

```markdown
# Live2D MCP

通用 Live2D 控制 MCP Server。让任何 MCP 兼容的 AI agent（Claude Desktop、OpenClaw 等）通过标准协议控制 Live2D 角色的表情和动作。

## 快速开始

```bash
# 安装
npm install -g live2d-mcp

# 首次配置（指定模型路径）
live2d-mcp init

# 启动 MCP Server
live2d-mcp start
```

## 配置 Claude Desktop

在 Claude Desktop 的 `claude_desktop_config.json` 中添加：

```json
{
  "mcpServers": {
    "live2d": {
      "command": "live2d-mcp",
      "args": ["start"]
    }
  }
}
```

## 可用工具

| 工具 | 参数 | 说明 |
|------|------|------|
| `live2d_expression` | `name` | 切换表情（从模型自动发现 + 内置 smile/bigsmile/sad/kongbai） |
| `live2d_motion` | `name` | 播放动作组（从模型自动发现） |
| `live2d_status` | — | 查询模型名、表情/动作/动画列表 |
| `live2d_parameter` | `params[]` | 直接操控 Cubism 底层参数 |
| `live2d_animate` | `name` | 播放语义动画（wink/nod/shake_head 等） |

## 配合渲染器使用

live2d-mcp 启动时会在 `ws://127.0.0.1:9228` 广播渲染指令。任何 WebSocket 客户端都可以接收并执行。

当前支持的渲染器：
- 浏览器页面（PIXI.js + Cubism SDK for Web）— 从 personal-agent 项目的 `live2d-view` 扩展复用
- 桌面窗口（计划中）

## 配置文件

`~/.live2d-mcp/config.json`：

```json
{
  "model": {
    "path": "/absolute/path/to/your-model.model3.json"
  },
  "ws": {
    "port": 9228,
    "host": "127.0.0.1"
  }
}
```

## 模型要求

- Cubism 3 或 Cubism 4 格式的 `.model3.json`
- 支持 Live2D Cubism SDK for Web 的表达式文件（`.exp3.json`）
```

- [ ] **Step 2: 提交**

```bash
git add -A && git commit -m "docs: README — 安装/配置/工具说明"
```

---

### Task 13: 端到端验证

- [ ] **Step 1: 全量 TypeScript 检查**

```bash
cd D:\claude\live2d-mcp && npx tsc --noEmit
```
Expected: PASS (无错误)

- [ ] **Step 2: 运行集成测试**

```bash
cd D:\claude\live2d-mcp && npx tsx test/test.ts
```
Expected: 所有测试通过

- [ ] **Step 3: 手动 MCP 协议测试（echo 模拟 STDIO）**

```bash
echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","clientInfo":{"name":"test"}}}' | npx tsx src/cli.ts start
```

Expected: 输出 initialize 响应 JSON，包含 `serverInfo.name: "live2d-mcp"` 和 `capabilities.tools: {}`

- [ ] **Step 4: 提交（如有修改）**

```bash
git add -A && git commit -m "chore: 端到端验证通过" || echo "无需提交"
```
